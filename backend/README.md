# SounTabeeb Backend

Express + MongoDB backend for SounTabeeb.

Quick start:

1. Copy `.env` to `.env` and set `MONGO_URI` and `JWT_SECRET`.
2. Install dependencies: `npm install`.
3. Run: `npm run dev` (requires `nodemon`) or `npm start`.
